# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Runtime / experiments
.runs/
# Recorded hardware runs brought home from a rented instance. Data, sometimes large,
# and regenerable from the seed -- but the OUTPUTS are not, so keep the tarballs too.
runs/
*.tar.gz
results/
*.tsv
wandb/

# GPU build artifacts
*.so
*.o
*.cubin

# IDE / OS
.DS_Store
.idea/
.vscode/
*.swp

# Reference PDFs (large; use manifest + local download)
reference/**/*.pdf

# Secrets
.env
.env.*

# Agent worktrees
.claude/worktrees/
